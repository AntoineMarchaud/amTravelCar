<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Theme.TravelCar" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/light_blue_400</item>
        <item name="colorPrimaryVariant">@color/light_blue_600</item>
        <item name="colorOnPrimary">@android:color/white</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>

        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowLightNavigationBar" tools:ignore="NewApi">true</item>
        <item name="android:windowBackground">@color/grey_5</item>
        <item name="actionBarStyle">@style/AppTheme.Toolbar</item>

        <item name="android:windowContentTransitions">true</item>

        <!-- specify shared element transitions -->
        <item name="android:windowSharedElementEnterTransition">
            @transition/transform_car
        </item>
        <item name="android:windowSharedElementExitTransition">
            @transition/transform_car
        </item>

    </style>

    <style name="AppTheme.Toolbar" parent="@style/Widget.AppCompat.ActionBar">
        <item name="background">@color/light_blue_400</item>
    </style>

</resources>